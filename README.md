# Media Photo Workbench / 融媒体图片工作台

## 项目简介

Media Photo Workbench / 融媒体图片工作台 是面向校园融媒体中心、新闻中心和影像部门的 Windows 桌面端图片工作台。

它用于活动现场图片导入、筛选、修图流转、客户端协作、导出发布和归档管理。系统以一台主机为中心，集中保存图片仓库和 SQLite 数据库，多台客户端设备可在同一局域网内上传图片、查看图片墙、打星、分类、修改状态、下载待修包并回传已修图。

本项目不是云盘，也不是实时传图系统。当前重点是本地主机集中存储和局域网协作。

## 当前版本

- 当前版本：`v1.0.0-rc.1`
- 发布类型：Windows ZIP 便携发布候选版
- 未来 Git tag：`v1.0.0-rc.1`
- 未来 Release 标题：`v1.0.0-rc.1 - Windows ZIP Portable Release Candidate`
- Release 类型：Pre-release

## 推荐下载方式

推荐使用 Windows ZIP 便携包：

```text
MediaPhotoWorkbench-v1.0.0-rc.1-x64.zip
```

当前不推荐 NSIS 安装包，也不提供 Web Installer。正式发布前会继续以 ZIP 便携包作为主要交付方式。

## 使用方式

1. 下载 `MediaPhotoWorkbench-v1.0.0-rc.1-x64.zip`。
2. 将 ZIP 解压到非系统盘目录，例如 `D:\MediaPhotoWorkbench`。
3. 双击运行 `Media Photo Workbench.exe` 或 `融媒体图片工作台.exe`。
4. 不要直接在压缩包内运行程序。
5. 首次使用建议先进入系统设置，选择图片仓库路径。

## 主机端快速开始

1. 启动软件，选择“启动为主机”。
2. 进入“系统设置”，设置图片仓库路径。
3. 进入“活动管理”，创建并激活活动。
4. 进入“导入图片”，导入 JPG / JPEG / PNG 图片。
5. 在主机首页查看本机地址、局域网地址和二维码。
6. 等待客户端通过地址或二维码连接。

主机端负责活动、仓库、数据库、图片处理、任务中心、导出发布和归档。

## 客户端快速开始

1. 确认客户端设备与主机在同一局域网，或连接到主机开启的 Windows 热点。
2. 通过主机首页显示的地址或二维码打开客户端页面。
3. 设置设备名，例如“修图电脑A”或“摄影组1号”。
4. 选择活动并上传 JPG / JPEG / PNG 图片。
5. 在客户端图片墙进行查看、打星、分类和状态修改。
6. 在客户端修图任务页下载待修包并回传已修图。

客户端设备名会保存在本机浏览器 / 客户端 localStorage 中，用于主机识别上传来源和操作记录。

## 推荐网络方式

- 同一 Wi-Fi 或有线局域网可用时，直接使用主机首页显示的局域网地址。
- 校园网可能存在设备隔离，即使连接同一个 Wi-Fi 也可能无法互访。
- 如果校园网无法连接，推荐主机开启 Windows 热点，其他设备连接该热点。
- Windows 热点常见主机地址为 `192.168.137.1`，实际端口以主机首页显示为准。
- 必要时检查 Windows 防火墙是否允许本软件访问专用网络。

## 支持格式

当前支持：

- JPG
- JPEG
- PNG

暂不支持：

- RAW / NEF / ARW / CR3
- HEIC
- 视频
- GIF / WebP / TIFF 原图导入

已修图回传建议使用 JPG / JPEG 成片。

## 核心功能

- 主机选择文件夹导入图片。
- 主机选择单张或多张图片文件导入。
- 主机拖拽图片或文件夹导入，文件夹当前只扫描第一层。
- 客户端单张、多张和拖拽上传图片。
- 图片墙分页、缩略图、预览图、筛选和搜索。
- 星级、状态、分类、备注。
- 批量状态、批量星级、批量分类。
- 批量操作后清空或保留选择的配置。
- 图片墙和预览弹窗快捷键提示。
- 任务中心统一展示导入、上传、下载、导出、待修包、已修图回传和归档任务。
- 待修包生成、平均拆包、自定义分包和待修包删除。
- 客户端下载待修包和上传已修图。
- 发布导出和批量 ZIP 下载。
- 活动归档、只读历史、活动回收站和图片回收站。
- 数据库手动备份、启动自动备份和数据库位置迁移。
- 在线客户端数量和客户端列表。
- 上传来源、上传者、上传时间和操作操作者追踪。
- 主机首页最近上传、最近修改、删除恢复和任务完成动态。
- 故障排查入口、日志目录打开和诊断信息复制。

## 数据安全说明

- 图片仓库路径由用户选择，软件不会主动删除用户仓库根目录。
- 图片原图、缩略图、预览图、已修图、导出包和归档内容保存在图片仓库中。
- SQLite 数据库只保存元数据、文件路径、状态和操作日志，不存储图片二进制。
- 手动备份会生成到当前图片仓库的 `metadata/database-backups/`。
- 启动自动备份默认启用，并按节流策略生成自动备份。
- 数据库位置迁移会先备份，再复制数据库到新位置。
- 迁移失败不会修改配置。
- 旧数据库不会自动删除。
- 数据库备份、迁移和归档不会删除用户原图仓库。

## 常见问题

### 客户端无法连接怎么办

1. 确认主机端软件已启动，并已选择“启动为主机”。
2. 使用主机首页显示的真实地址和端口，不要使用旧截图、旧二维码或客户端本机的 `localhost`。
3. 确认客户端与主机在同一 Wi-Fi、有线局域网或 Windows 热点下。
4. 校园网可能存在设备隔离，同一 Wi-Fi 下也可能无法互访。
5. 校园网不通时，建议主机开启 Windows 热点，客户端连接热点后访问主机。
6. Windows 热点常见主机地址为 `192.168.137.1`。
7. 检查 Windows 防火墙是否允许本软件访问专用网络。
8. 仍无法连接时，在系统设置中复制诊断信息，并连同客户端错误提示一起发送给维护人员。

### 图片无法预览怎么办

1. 确认图片原图仍在仓库中，没有被手动移动或删除。
2. 确认导入任务或客户端上传处理任务已经完成。
3. 尝试刷新图片墙。
4. 查看任务中心是否有缩略图或预览图生成错误。
5. 如果是归档活动，只读历史依赖归档缩略图，需确认归档目录仍存在。

### 导入失败怎么办

1. 确认图片格式为 JPG / JPEG / PNG。
2. 确认仓库路径存在且可读写。
3. 确认源文件未被其他程序占用。
4. 查看任务中心错误列表。
5. 同一活动内重复图片会计入 skipped，不属于失败。

### 任务失败怎么办

1. 打开任务中心查看失败任务。
2. 展开错误列表查看文件名和原因。
3. 修复路径、权限或文件占用问题后重新执行。
4. 对导入、上传、回传等处理类任务，取消或失败不会删除已成功处理的图片。

### 如何查看日志

进入“系统设置 / 故障排查”，点击“打开日志目录”。

### 如何复制诊断信息

进入“系统设置 / 故障排查”，点击“复制诊断信息”。诊断信息包含版本、运行模式、端口、数据库路径、仓库路径、局域网地址和当前活动摘要。

### 为什么校园网无法连接

校园网经常启用设备隔离、跨网段限制或防火墙策略。即使主机和客户端连接同一个 Wi-Fi，也可能无法互相访问。现场使用时建议优先准备 Windows 热点方案。

### 为什么推荐 Windows 热点

Windows 热点能让主机和客户端处在更可控的小型局域网内，减少校园网隔离、防火墙和跨网段问题。热点场景下主机常见地址为 `192.168.137.1`。

## 已知限制

- 当前推荐 ZIP 便携包。
- 暂不提供 NSIS 安装包。
- 暂不支持单文件 portable exe。
- 暂不支持 RAW / HEIC / 视频。
- 暂不支持远程传输和云同步。
- 暂不提供账号、登录和权限系统。
- 客户端拖拽文件夹上传暂不支持。
- 主机拖拽文件夹导入当前仅扫描第一层。
- 手机和平板浏览器只作为轻量访问入口，不覆盖完整批量上传、批量下载、导出、归档和永久删除流程。

## 开发与构建

开发环境建议：

- Windows 11
- Node.js v22.22.2 LTS
- pnpm
- VS Code

常用命令：

```bash
pnpm install --registry=https://registry.npmmirror.com
pnpm dev
pnpm build
pnpm dist:portable
```

`pnpm dist:portable` 会生成 Windows ZIP 便携包到 `release-pack/`。本轮 `v1.0.0-rc.1` 仅准备发布候选文档和版本信息，不自动打包、不打 tag、不创建 GitHub Release。

## 不应提交到 Git 的内容

以下内容不得提交：

- `dist/`
- `dist-server/`
- `release/`
- `release-pack/`
- `release-win/`
- `data/`
- `logs/`
- `config/config.json`
- `working/`
- `archive/`
- 数据库文件 `*.db`、`*.db-wal`、`*.db-shm`
- ZIP / EXE 构建产物
- 真实图片仓库和真实活动图片

## 后续路线

- `v1.0.0-rc.1`：正式版候选，功能冻结，进入完整回归测试和发布准备。
- `v1.0.0`：正式稳定版，取决于 `v1.0.0-rc.1` 回归结果。
- `v1.0.0` 之后再评估远程传输、安装器、RAW / HEIC / 视频、权限系统和更复杂的统计复盘。
