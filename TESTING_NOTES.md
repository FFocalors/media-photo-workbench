# Media Photo Workbench / 融媒体图片工作台 - 测试记录

## v1.2.0-alpha.1 重构前基线（阶段一）

- **时间**：2026-07-15；分支 `dev/v1.2.0`；基线提交 `99e6f9fac487490267839c62a05db82ed5e6ccdb`；起始版本 `v1.1.0-alpha.4`。
- **`pnpm build`**：通过，退出码 0，34.3 秒；前端 TypeScript、服务端 TypeScript 和 Vite 构建均成功。唯一已知警告为主 JS chunk 701.87 kB 超过 500 kB，本轮阶段一不改变业务拆包策略。
- **`pnpm test:camera-ftp`**：通过，退出码 0，98.4 秒。IIS logic 37 项、provisioning 30 项、elevated PowerShell 11 项、PowerShell contract 27 项、UI 40 项、watcher 14 项，共 159 项通过；错误级日志来自预期的 Mock 失败路径。
- **`git diff --check`**：通过，退出码 0，无输出；基线结束时工作区干净。
- **阶段一变更后复测**：`pnpm build` 以 `1.2.0-alpha.1` 通过（23.2 秒，既有主 JS chunk 701.91 kB 警告）；`pnpm test:camera-ftp` 再次通过 6 个子套件、159 项（96.7 秒）；`git diff --check` 通过，Git 状态只有 10 个预期的版本/文档文件，没有构建或测试产物。
- **真实 IIS 边界**：测试只执行一次非提权、只读 `iis-ftp-status.ps1`。未调用真实 setup、adopt、control、credentials，未触发 UAC，provisioning 明确为 `fixtureOnly=true / realIisMutation=false`；其他脚本使用 Mock、临时目录、parser 或 dot-source。
- **环境限制**：只读探测确认 FTPSVC 为 Running/Auto，但普通权限无法完整读取 IIS 站点配置，因此返回 `unknown / requiresAdmin=true`。不能据此宣称真实站点配置完整通过，后续保留管理员只读检测和真机上传为人工验收项。
- **当前风险**：既有主 JS chunk 体积警告；真实 IIS/ACL/防火墙/账户状态未由自动测试修改或完整确认；压力测试和多设备接入测试不在本轮执行。

## v1.2.0-alpha.1 阶段二：状态真相与启动恢复

- 普通检测、管理员完整检测与响应时间分别断言；普通 partial 不覆盖为伪 full，较旧轮询不覆盖较新管理员结果，前端只保存管理员结果作为历史参考。
- 启动恢复矩阵覆盖：合法活动与精确 IIS 路径恢复、无效活动、仓库断开、目录缺失、IIS 路径不一致、partial 但路径有明确证据，以及停机文件恰好补扫一次。测试断言不创建缺失目录、不请求 UAC、不执行系统修改入口。
- 阶段门禁：`pnpm build`、`pnpm test:camera-ftp` 与 `git diff --check` 通过；唯一构建提醒仍是既有主 JS chunk 体积警告。

## v1.2.0-alpha.1 阶段三：故障注入

- watcher 临时目录覆盖：未完成传输退出、稳定检测时退出/切换、导入时切换、目录运行中消失、停机新增补扫、历史回执去重、同路径同 size/mtime 内容覆盖、数据库失败可重试、任务记录失败、Socket 广播失败，以及 drain timeout 显式返回。
- 临时 SQLite/仓库覆盖：数据库 BEFORE INSERT 失败不留图片行、不写成功回执且保留原图；缩略图注入失败、损坏 JPG、EXIF 失败降级、任务完成记录失败和导入后广播失败均按合同验证。
- PowerShell Mock/源码合同覆盖：UAC 取消、timeout、无结果、无效 JSON、缺失结果字段、端口冲突、ACL SDDL 回滚复读、站点/监听失败后的运行状态与 FTPSVC 回滚，以及 partial rollback 不误报完整恢复。
- 所有故障测试使用 Mock、临时目录、临时数据库或普通只读检测；不得调用 setup/repair/adopt/control/credentials 的真实修改入口，不修改真实 IIS、账户、ACL、防火墙或服务。

## v1.2.0-alpha.1 阶段四：模块边界兼容回归

- 前端回归直接编译错误呈现新模块，并断言旧 `cameraFtpUiState` facade 与新模块导出为同一实现；最近文件、配置反馈和错误卡的源码合同改为检查实际子组件，不再强制文案留在 1900 行级主面板。
- 后端回归断言 `cameraFtpOrchestrator` 的活动切换执行器、`cameraFtpWatcher` 的候选分类/记录合并纯函数、`iisFtpManager` 的 unknown/normalize 状态函数均与新模块保持 facade identity；消费者导入路径和 HTTP/Socket 合同未改变。
- watcher 模型边界不引入文件系统或 hash IO；IIS status 模块不执行 PowerShell；活动切换事务使用 hooks 验证成功、逐阶段失败和回滚。所有真实系统交互仍由既有适配层负责。
- 阶段门禁：`pnpm test:camera-ftp` 7 个子套件通过（96.6 秒）；`pnpm build` 通过（28.7 秒）；`git diff --check` 通过。Vite 唯一提醒仍为既有主 JS chunk 703.63 kB；测试未触发 UAC 或真实 IIS/账户/ACL/防火墙/服务写入。

## v1.2.0-alpha.1 阶段五：状态和错误语义回归

- 纯状态矩阵断言：success 绿色；running/receiving/processing/importing/pending/waiting 蓝色；skipped/duplicate/warning 黄色；failed 红色；unknown/admin_required/stopped/cancelled 灰色。矩阵额外断言只有 `failed` 能映射到 danger。
- 消费端源码合同断言任务中心、图片墙、FTP 最近文件和 FTP 面板均导入共享状态模块，provisioning 使用共享计划语义；废片是 neutral，计划 blocked 是 warning，重复图片不显示为上传失败。
- FTP UI 回归覆盖 partial/ADMIN_REQUIRED 中性状态、停止但活动仍关联、watcher 与 FTP 服务事实分离、零值统计隐藏、同路径记录合并和 ConfirmDialog 小窗口滚动合同。
- API error 回归覆盖新顶层字段、旧 details 原样保留、顶层 rollbackStatus/operationId 优先、旧 flags 回退、中文 impact/nextAction 以及嵌套密码和 technicalDetails 脱敏。
- 阶段门禁：`pnpm test:camera-ftp` 8 个子套件通过（111.8 秒）；`pnpm build` 通过（15.0 秒）；`git diff --check` 通过。唯一构建提醒仍是既有主 JS chunk 706.86 kB；测试未修改真实 IIS、账户、ACL、防火墙或服务。

## v1.2.0-alpha.1 阶段六：重启恢复和数据安全回归

- **配置安全（9 项）**：覆盖显式 schemaVersion、旧配置幂等迁移、FTP 明文密码递归清理、损坏 JSON fail closed、旧/当前 schema 字段类型损坏拒绝且原文件字节不变、未来 schema 拒绝、迁移替换失败保留原文件、普通保存失败保留磁盘与内存旧配置。全部使用临时目录。
- **数据库安全（10 项）**：覆盖迁移账本、重复打开幂等、高风险迁移备份与 quick_check、重复初始化拒绝、迁移失败不发布连接/不登记成功、账本与实际 schema 漂移 fail closed、表重建事务回滚、失败备份最多保留 3 份，以及回执跨重启/逻辑删除/归档保留、永久删除显式清理与外键级联。全部使用临时 SQLite。
- **活动永久删除（6 项）**：覆盖仓库外路径拒绝、数据库末端触发器失败后完整回滚、提交后隔离目录删除失败返回部分成功、重启后继续清理，以及模拟提交前/提交后进程中断时分别恢复 working/archive 或完成隔离目录清理。journal 和所有文件仅位于临时仓库。
- **日志轮转（7 项）**：覆盖阈值前保留、阈值处启动前轮转、同时间戳唯一文件名、只清理工作台历史日志、非普通文件保护、logger 打开后保护当前日志和初始化顺序；测试不读取或输出日志正文。
- **诊断与 operationId（9 项）**：覆盖安全的客户端 correlation id、非法 header 替换、统一错误继承请求 id、参数校验错误完整结构、成功 status/diagnostics 轮询不覆盖最近业务操作、复合敏感键/完整路径脱敏、白名单诊断字段、密码/SecureString/最近文件名/其他站点信息排除，以及 API → orchestrator → PowerShell 父子 id → 前端链路合同。UI 另以行为测试覆盖父子 ID 展示、默认折叠详情和损坏 JSON 保留响应头 operationId。
- **既有重启与一致性回归**：watcher 套件继续覆盖退出/恢复、停机新增、未变化历史文件跳过、内容变化重新判断、activeEventId/仓库/IIS 路径失效时不恢复；fault/UI/IIS 套件继续覆盖数据库写失败、任务/Socket 旁路、UAC 取消/超时、rollback partial/failed 不误报成功。
- **自动化边界**：所有新增用例仅使用纯逻辑、Mock、临时目录和临时数据库；没有调用真实 IIS 修改脚本，没有修改账户、ACL、防火墙、FTPSVC 或站点。

## v1.2.0-alpha.1 任务中心与全局瞬时通知回归

- 侧栏任务中心关闭时箭头向右、展开时箭头向左；点击入口或面板内部不应误关，点击页面其他空白区域或按 `Esc` 应收回。
- 侧栏任务面板必须通过 `document.body` portal 脱离图片墙 stacking context；图片卡片必须建立独立 stacking context，悬浮按钮、状态标签和图片内容不得覆盖任务面板。
- 操作结果统一使用 `TransientNotice`：主机概览、活动、导入、图片墙、修图、导出、归档、设置、客户端连接/上传/修图及相机 FTP 普通反馈均须接入；静态系统状态、进行中任务、结构化错误操作卡和排查说明不得被自动清除。
- 瞬时通知按语义自动消失：成功/信息 5 秒、警告 7 秒、错误 9 秒。进入采用 200ms 减速淡入和轻微位移，退出采用 150ms 加速淡出；`prefers-reduced-motion` 下禁用视觉过渡。新通知替换旧通知时，旧计时器不得清除新通知。
- 自动回归由 `pnpm test:status-semantics` 覆盖源码合同；`pnpm lint`、`pnpm build` 和 `git diff --check` 作为本轮门禁。真实 Electron 窗口的点击、视觉层级和渐隐观感仍需人工补验。

### 下一阶段人工压力与多设备清单（本轮未执行）

1. 在隔离测试主机上以 50 / 300 / 500 张 JPG/JPEG 分批上传，记录落盘、稳定检测、thumb/preview、SQLite 和页面出现的总耗时、CPU、内存、磁盘与日志增长。
2. 两台及以上相机/客户端使用不同文件名前缀并发上传，验证同名覆盖提示、队列计数、活动切换阻断和断线重连；不得使用生产活动或唯一原图。
3. 上传进行中分别执行应用退出、Windows 重启、网络中断和仓库盘短暂离线；恢复后确认停机新增补扫、未变化历史文件不重导、变化文件重新判断，且无成功假记录。
4. 在专用测试机人工验证默认与自定义控制端口、WLAN/Windows 热点、UAC 取消/超时、IIS 站点停止后活动仍关联、setup/repair/adopt 的确认与完整 rollback；执行前导出现有 IIS 配置并确认无无关站点受影响。
5. 用主机、两台 Windows 客户端和一台手机/平板同时查看、打星和修改状态，核对 Socket.IO 顺序、最终一致性、角色限制和 1200px/缩放窗口可用性。
6. 压力后检查 `schema_migrations`、`camera_ftp_file_receipts`、任务/操作日志、历史日志保留数量和脱敏诊断；抽样比对原图 hash，确认没有移动、覆盖或删除原图。

## v1.1.0-alpha.4 相机 FTP 运行状态与接收记录视觉回归

- 重启回归：成功导入和重复跳过均写入文件处理回执；关闭并重新启动 watcher 后，路径、大小、修改时间未变化的文件不得再次创建任务，发生变化的同路径文件仍应重新处理。旧库首次升级时，已有 `camera_ftp` 图片记录应直接引导回执而不重复创建导入任务。回执表按活动级联清理且不得包含密码。
- 密码状态回归：已设置密码时刷新页面仍显示“FTP 密码已设置”，不显示容易误解的空密码框；只有点击“修改账户或密码”才显示新密码/确认密码输入。任何页面状态、配置、SQLite、日志和测试快照均不得包含真实密码。
- 同一路径的并发文件系统通知必须只建立一个候选处理入口；若历史中同时存在旧 `receiving` 和较新的 `imported/skipped/failed`，状态接口只返回最新有效状态，不得在 pending/queued/importing 均为 0 时继续显示陈旧“等待传输完成”。
- watcher 停止或活动切换必须清理旧活动非终态展示记录；真正的新一轮同路径上传仍应以新的接收状态覆盖旧终态，并可再次导入。
- FTP 运行中时顶部必须显示“相机 FTP 已启动 / 运行中”，并在首屏给出当前活动、地址、实际控制端口、用户名、接收目录与最近接收时间；账户、端口和高级 IIS 管理默认折叠，停止与重启仍可直接操作。
- 最近记录状态合同：`receiving/waiting/importing` 使用蓝色，`imported` 使用绿色，`skipped` 使用黄色或灰色，只有 `failed` 使用红色；统计为 0 时隐藏，单行不重复显示活动名和通用状态原因，错误详情仅在失败时展开。
- 相机连接活动仅根据 watcher 的真实文件活动推断：处理中显示“正在传输”，最近 5 分钟有接收记录显示“最近有相机连接”，超时恢复“暂无相机连接活动”。不得把该推断展示为实时在线，也不得伪造连接数或客户端 IP。
- 页面及复制参数使用通用“相机 FTP”表述；Nikon Z6III 只作为已验证设备说明。管理主操作统一为“配置并启动 FTP / 自动配置并启动 FTP”，不再使用旧的“自动修复”命名。
- 响应式人工回归覆盖 1920×1080、1366×768 以及 Windows 125%/150% 缩放：顶部按钮可见、状态卡不横向溢出、长路径省略并保留 title、最近记录列表独立滚动。

## v1.1.0-alpha.4 FTP 接收活动原子切换回归

- 现场切换 `ftp测试2 → ftp测试3` 的 IIS 子操作均成功：停止站点 operation `d9f4c5bd-6b3f-4ded-a5ce-c633e6b87de5`、路径更新 `9123a03b-e840-4181-8a85-7949f5a68fc0`、目标启动 `ed8b1a8c-c643-4afb-84b4-f7e9cc007685`。目标 Site ID 3、`*:1024:`、新 physicalPath、Started 和 FTPSVC 监听均正确。
- 真实失败是 Node 验证复用了路径更新操作中“重启前 Stopped”的陈旧 `systemStatus`，没有使用目标启动操作返回的 Started 状态，因而误报“站点未恢复启动”。旧实现没有父 operationId，页面阶段退化为未知；随后 IIS 路径、watcher、站点和旧 `activeEventId` 实际均回滚成功。
- 新回归覆盖运行站点成功切换、停止站点保持停止、中文/空格路径、目标目录准备、上传阻断、互斥锁，以及 ACL、physicalPath、watcher、站点重启、最终验证和 activeEventId 提交失败。每个失败场景验证旧路径、旧 watcher、旧站点状态和旧活动关联；另覆盖回滚项失败时返回 partial 而非误报成功。
- 前端回归覆盖待选择值与真实当前活动分离、切换中暂停/屏蔽轮询结果、失败后强制读取服务端真实状态、中文阶段与父 operationId/回滚摘要显示。
- 自动测试只使用纯事务 hooks、临时目录、PowerShell parser 与只读 IIS 状态；不执行真实活动切换，不修改 IIS、账户、防火墙或真实图片。

## v1.1.0-alpha.4 IIS 数值枚举验证与防火墙回滚回归

- 现场 operation `6b7e6fe2-6c97-4fe1-a1e7-5d4140901a83` 的 Apply 阶段全部成功，最终验证只把 SSL `0/0`、Authorization `accessType=0`、`permissions=3` 误判为失败；本机 `FTP_schema.xml` 证明这些值分别等于 `SslAllow/SslAllow`、`Allow`、`Read + Write`。
- PowerShell 5.1 纯逻辑回归覆盖数值与名称两种枚举表示，并检查最终验证输出具体 code、expected、actual 和 raw 值。
- 同一 operation 的回滚只在控制端口防火墙规则失败：旧逻辑把 Boolean `True` 传给 NetSecurity `Enabled` 枚举。测试现确认使用字符串枚举值，并在恢复后逐字段比对防火墙快照；不执行真实规则修改。
- Node 状态提交回归覆盖 watcher 启动、配置保存、`activeEventId / managedSiteId / controlPort / passivePorts / watcher target` 最终验证，以及验证失败后配置和 watcher 快照的逆序恢复。
- UI 回归覆盖多个具体失败码的中文合并展示和脱敏技术详情，不再只显示“最终配置验证未通过”。

## v1.1.0-alpha.4 非规范 ACL、验证回滚与弹窗适配回归

- 现场只读证据：目标相机 FTP 目录为 protected、non-canonical DACL，并包含已删除本地账户的孤立 SID；内存调用原修改方法稳定返回 `0x80131501`，确认失败位于 Windows ACL 修改而非 IIS、OneDrive 同步或文件占用。
- PowerShell 合同：覆盖非规范 ACL 规范化重建、Deny/Allow 顺序、SDDL 修改前快照、临时目录写入与精确恢复、回滚失败保留托管账户、ACL 失败 HRESULT/owner/protected/canonical 诊断和最终验证失败项。
- UI 合同：覆盖 ACL 中文错误与 partial rollback、failedStep/rollback 技术详情、完整计划/低优先级提示折叠、确认弹窗 viewport 高度与滚动区域、repair 缺账户时携带一次性密码，以及普通轮询不覆盖管理员已确认字段。
- 安全边界：自动测试只使用内存 ACL、临时目录、fixture 和只读 IIS status；不会修复或清理真实 `E:\OneDrive\Desktop\仓库2` ACL，也不会创建/删除真实账户、站点或防火墙规则。

## v1.1.0-alpha.4 IIS FTP 统一 Provisioning 回归

本轮自动测试只使用纯 planner fixtures、内存 fake runtime、临时目录 watcher、源码合同和普通只读状态，不调用真实 IIS mutation 脚本。

```powershell
pnpm build:server
pnpm exec tsc -p tsconfig.json --noEmit
pnpm test:camera-ftp
pnpm build
git diff --check
```

- provisioning planner 覆盖 30 个场景：全新环境、幂等重跑、功能缺失、停止站点、错误 binding、其他 IIS/普通进程/保留端口冲突、账户缺失/冲突、ACL 缺失/过宽、PASV、防火墙、FTPSVC、repair+start、回滚/部分回滚、UAC 取消、端口同步、外部资源不变、旧目录保留。
- PowerShell 5.1 fake runtime 同时验证 `Started/Stopped` 字符串和 `1/3` 数值枚举，固定“实际已启动却因 state=1 判失败”的回归。
- 只读边界验证 provisioning-plan / discover-sites 不创建目录、不保存 config、不启动 watcher；解除活动关联使用实时管理器状态，partial/unknown 必须提权复检，禁止 last-known 缓存参与安全决策。
- Node 提交边界验证统一 IIS reconcile 在 watcher 之前完成；setup/repair/adopt/start/restart 的 watcher/config 失败恢复原快照，不保存半配置应用状态。
- 管理员事务验证 Windows RestartNeeded 使用 `WINDOWS_RESTART_REQUIRED` 暂停；失败 JSON保留 preflight、plan、completedSteps、failedStep、rollback items/warnings，且密码在嵌套诊断中仍被脱敏。
- 前端验证七阶段进度、六种计划状态、四类提醒、阻塞计划禁止 Apply、ACL/PASV/接管高风险确认、推荐端口、重试重新生成计划和结构化技术详情。
- 人工真机验收仍需从工作台执行：活动 → 用户名/密码 → 控制端口 →“配置并启动 FTP”→ 确认计划 → 接受一次 UAC；自动测试本身不会弹 UAC 或改变真实站点/账户/ACL/防火墙。

## v1.1.0-alpha.4 IIS FTP 专用启停与启动失败诊断回归

- 日志与现场只读检查确认 `FTPSVC` 为 `Running/Automatic`，端口 22 未监听；失败来源是目标 IIS FTP 站点运行时，而不是 Windows 服务本身。
- PowerShell fake runtime 覆盖 `ftpServer.Start/Stop`、`ftpServer.state` 和独立退出码，静态合同禁止恢复通用 `$Site.Start/$Site.Stop/$Site.ServerAutoStart`。
- setup、adopt、start、restart 均检查 `start_ftp_service -> start_ftp_site -> verify_ftp_listener` 阶段顺序；回滚继续只处理工作台管理的 IIS、ACL、防火墙和账户内容。
- UI 回归覆盖 `IIS_FTP_SITE_START_FAILED` 中文主体、阶段、非破坏性建议、HRESULT、FTPSVC 状态和回滚警告；技术详情继续进行密码脱敏。
- 自动测试不调用真实 IIS setup/adopt/start/stop/repair；真机复测需要用户在工作台确认摘要并接受 UAC。

## v1.1.0-alpha.4 IIS FTP 可配置端口、冲突与自动修复回归

自动验证：

```powershell
pnpm test:camera-ftp-elevated
pnpm test:camera-ftp-powershell
pnpm test:camera-ftp-ui
pnpm test:camera-ftp-iis
pnpm test:camera-ftp-watcher
pnpm build
```

- 提权 IPC：覆盖中文/空格路径、延迟生成结果、分段 JSON 写入、非零退出但有结构化结果、非零退出无结果、无效 JSON、超时、UAC 取消映射、临时目录清理和密码脱敏。
- PowerShell 5.1：解析全部 `scripts/windows/*.ps1`，验证动态 `*:{controlPort}:` binding、动态控制/被动防火墙规则、Windows 保留端口、非 IIS 进程、IIS 站点冲突、可用端口建议和 ACL 检测合同。
- 真实失败回归：setup 曾在 `configure_iis_site` 阶段由 `Get-MpwGlobalPassivePorts` 读取全局 `system.ftpServer/firewallSupport.externalIp4Address` 而触发 `0x80070585 无效索引`；全局层现在只读写 `lowDataChannelPort/highDataChannelPort`，站点级外部地址仍由站点配置处理。
- FTP 授权 schema 回归：22 端口通过冲突预检后曾在 `configure_iis_site` 调用站点内嵌 `$security.GetChildElement('authorization')`，IIS 返回“无法识别的元素 authorization”。现在所有读取、配置、账户更新和回滚均使用站点作用域 `system.ftpServer/security/authorization` 配置节；静态合同禁止恢复错误的子元素访问。
- 防火墙规则回归：旧代码用 `Set-NetFirewallRule -Name ... -DisplayName ...` 组合互斥参数集；现在必须使用 `-NewDisplayName`。测试覆盖旧本地规则二次确认、`PolicyStoreSourceType != Local` 拒绝修改、更新快照与后续失败恢复，且禁止恢复错误参数组合。
- 权限边界回归：端口未监听但普通权限不能读取 IIS binding 时必须显示“需管理员确认”，显式检测发送 `fullInspection: true` 并请求只读 UAC，禁止误报“可用”。
- OneDrive 路径：带 `ReparsePoint` 但没有 `LinkType/Target` 的 Files On-Demand 目录允许作为 FTP 根目录；真实 SymbolicLink/Junction 仍必须拒绝。实际 `仓库2\working\ftp测试2\原图\相机FTP` 已通过只读校验。
- 前端状态机：未初始化时主操作为“配置并启动 FTP”；控制端口实时校验，冲突卡显示端口、PID/进程或 IIS 站点以及候选端口；按钮文字可换行、禁用态与可点击态高对比，UAC 取消后表单和重试入口仍保留。
- 自动修复边界：只修改托管站点、托管账户、当前活动 ACL、工作台防火墙规则和 IIS 服务器级被动端口；不得自动接管、停止其他程序、删除其他 binding 或修改无关 IIS 站点。
- 安全边界：所有自动测试使用临时脚本或只读检测，不触发 UAC，不执行真实 setup/adopt/start/stop/restart/repair，不修改或删除现有 IIS 测试资源。

人工验证顺序：完整退出并重启工作台，普通刷新应返回 partial 成功状态；选择活动、填写账户和控制端口后点击“配置并启动 FTP”。分别验证默认 21、普通程序占用、无关 IIS 站点占用、手动改用 22 或候选端口、控制端口落入被动范围、UAC 取消重试。若本机存在旧版 GUID 防火墙规则或宽泛 ACL，计划确认必须列出风险与资源边界；取消不得修改系统。22 成功后检查站点 binding、FTP Authorization Rules、控制端口防火墙规则和相机参数均使用实际配置，并确认自动配置未改变无关站点或策略规则。

## 历史：v1.1.0-alpha.3 Windows IIS FTP 迁移测试清单

> 本节记录 alpha.3 迁移时的固定端口与首次配置验收条件，不是 v1.2.0 当前操作合同。现行控制端口默认 21、可在 1-65535 配置，站点接管始终要求用户明确确认。

- **架构**：仅使用 Windows IIS FTP 单一服务架构。
- **路径**：`working/{event_slug}/原图/相机FTP/` 同时是 IIS 根目录、原图最终目录和 watcher 目录。
- **端口**：控制端口 `21`，PASV `50000-50100`，binding `*:21:`。
- **网络**：同一 Wi-Fi 和 Windows 热点两种模式必须同时展示；`192.168.137.1` 仅是常见热点地址。
- **真机证据**：Nikon Z6III 已在 IIS FTP、端口 21、PASV `50000-50100` 下上传成功。
- **安全边界**：自动测试只运行纯逻辑、临时目录 watcher 和只读 IIS 检测；不运行 setup/repair/adopt/start/stop，不修改或删除现有站点、防火墙、账户、目录或图片。
- **安全提示**：普通 FTP 为明文协议，仅在可信局域网/Windows 热点测试，并使用不与 Windows、校园网或其他服务复用的专用密码。

自动验证：

```powershell
pnpm test:camera-ftp-iis
pnpm test:camera-ftp-watcher
pnpm build
```

2026-07-14 状态机完善回归：26 项 IIS/JSON IPC/部分检测/首次配置 UI/原地导入/服务与关联拆分断言，以及 10 项 `原图/相机FTP` watcher 断言全部通过；测试未出现 UAC，也未执行任何 IIS、服务、账户、ACL 或防火墙修改入口。

核心人工验收：

1. 普通启动应用不弹 UAC；普通检测返回 partial 时，活动、用户名、密码和“自动检测、修复并启动 FTP”仍可用，点击确认后自动请求 UAC。
2. 有权限读取 IIS 配置时，检测到唯一 `:21` IIS FTP 站点可在首次配置确认范围内自动接管；多个站点必须列出站点名、路径、binding 和状态供用户选择，第三方进程不得被自动结束。
3. 首次配置按“选择活动 → 填写用户名/两次密码 → 自动检测、修复并启动 FTP”完成，不要求先保存账户或管理员诊断；成功后密码框清空，失败时保留以便重试。
4. 切换 FTP 活动时，有未稳定或导入中文件应被阻止；成功后 IIS physicalPath 和 watcher 均指向新活动 `原图/相机FTP` 目录，切换时不要求重新输入全局账户。
5. 当前 FTP 活动不能归档、逻辑删除或永久删除；恢复活动不自动绑定。
6. 上传 JPG/JPEG 直接落入 `原图/相机FTP/`，稳定后不得复制或重命名原图；`original_path` 指向该文件，并生成 EXIF、thumb、preview、SQLite、任务和 `image-created`。
7. 退出应用只关闭 watcher，不停止 IIS 站点或 FTPSVC；再次启动只恢复 watcher，不自动初始化或接管。
8. 初始化、修复和接管确认页必须提示 PASV `50000-50100` 是 IIS 服务器级配置，可能影响本机其他 IIS FTP 站点。
9. 已被工作台接管/创建的站点执行系统修改前，必须匹配持久化的 IIS Site ID、站点名、FTP binding 和托管账户标记；同名但 ID 不同的站点只能重新显式接管。授权错误应显示为可修复健康项，而不是丢失所有权。
10. “停止 FTP”只停止站点并保留活动和 watcher；“解除活动关联”是独立操作，站点运行或存在上传任务时必须拒绝。
11. 多相机提示必须明确要求不同文件名前缀，避免同名覆盖。

## 历史相机 FTP 测试说明

IIS FTP 迁移前的接收服务、端口、目录和命令诊断测试步骤已从现行测试清单移除，避免与当前验证流程混用。需要追溯版本演进时查看 `CHANGELOG.md` 和 `DEVELOPMENT_LOG.md`；当前回归只执行上方 Windows IIS FTP 清单及后续完整回归。

## 历史：v1.1.0 开发阶段完整回归测试清单

- **历史版本**：v1.1.0-alpha.3
- **历史定位**：内部开发阶段，最终未发布，由 `v1.2.0-alpha.1` 接续
- **原计划交付物**：`MediaPhotoWorkbench-v1.1.0-x64.zip`（计划已取消）
- **原计划 Release 标题**：`v1.1.0 - Windows ZIP Portable Stable Release`（计划已取消）
- **当前用途**：仅保留为旧功能完整回归参考，不代表当前发布安排。
- **不纳入本轮**：远程传输、RAW / HEIC / 视频、账号权限、NSIS、Web Installer、云同步、大 UI 重构。

### A. 启动与基础

1. ZIP 解压运行。
2. 首次启动。
3. 设置仓库路径。
4. `/api/health` 正常。
5. 日志目录可打开。
6. 诊断信息可复制。

### B. 活动管理

1. 创建活动。
2. 激活活动。
3. 编辑活动。
4. 逻辑删除活动。
5. 回收站恢复。
6. 永久删除。

### C. 导入与上传

1. 主机选择文件夹导入 JPG / JPEG / PNG。
2. 主机选择图片文件导入。
3. 主机拖拽图片导入。
4. 主机拖拽文件夹导入。
5. 客户端上传单张。
6. 客户端上传多张。
7. 客户端拖拽上传。
8. 重复图片 skipped。
9. 大批量导入。
10. 导入任务取消。

### D. 图片墙

1. 图片分页。
2. 缩略图加载。
3. 预览图加载。
4. 星级。
5. 状态。
6. 分类。
7. 备注。
8. 批量状态。
9. 批量星级。
10. 批量分类。
11. 批量操作后选择行为。
12. 快捷键提示面板。
13. 筛选状态下预览改状态不退出。
14. 星级等于 / 及以上筛选。
15. 上传来源筛选。

### E. 协作

1. 客户端设备名。
2. 在线客户端数量。
3. 在线客户端列表。
4. 上传来源记录。
5. 图片元数据上传者显示。
6. 操作日志操作者显示。
7. 最近动态。

### F. 任务中心

1. 导入任务。
2. 客户端上传处理任务。
3. 批量 ZIP 下载任务。
4. 发布导出任务。
5. 待修包生成任务。
6. 已修图回传任务。
7. 归档任务。
8. 归档清理任务。
9. 任务取消。
10. 任务错误列表。
11. 下载入口。

### G. 修图协作

1. 生成待修包。
2. 自定义分包。
3. 删除误生成待修包。
4. 客户端下载待修包。
5. 已修图回传。
6. 回传后状态变为 edited。
7. 缩略图 / 预览图更新。

### H. 导出发布

1. 按原图导出。
2. 按预览图导出。
3. 按 best 导出。
4. 发布图导出。
5. 批量 ZIP 下载。
6. 缺失文件错误提示。

### I. 归档

1. 活动归档。
2. 归档只读打开。
3. 清理 working 工作区。
4. 归档删除。
5. 归档数据仍可查看缩略图。
6. 归档后原 working 清理不影响只读历史。

### J. 数据安全

1. 手动备份数据库。
2. 启动自动备份。
3. 备份文件生成在仓库 `metadata/database-backups`。
4. 数据库位置迁移。
5. 迁移失败回滚。
6. 旧数据库不删除。
7. 图片仓库不删除。

### K. 网络

1. 同一 Wi-Fi 连接。
2. Windows 热点连接。
3. 校园网隔离场景说明。
4. 防火墙拦截提示。
5. 二维码连接。

### L. 关闭与重启

1. 导入中取消。
2. 导入中关闭程序不报 JS error。
3. 关闭后重启数据保留。
4. 任务完成记录正常。
5. 数据库无损坏。

### 构建与后续发布准备

1. `pnpm build` 通过。
2. 原计划 ZIP 文件名 `MediaPhotoWorkbench-v1.1.0-x64.zip` 已取消，未生成正式发布包。
3. 原计划 Git tag `v1.1.0` 已取消，未创建。
4. 原计划 GitHub Release `v1.1.0 - Windows ZIP Portable Stable Release` 已取消，未创建。
5. 本清单不再作为发布批准依据；当前发布约束以 v1.2.0-alpha.1 基线为准。
6. 确认 `dist/`、`dist-server/`、`release/`、`release-pack/`、ZIP、EXE、数据库、日志、真实图片、`working/`、`archive/` 不进入 Git。

## v0.17.0 阶段测试汇总

- **状态**：17.1 与 17.2 已完成并测试通过。
- **确认时间**：2026-05-24。
- **构建验证**：最近一次 `pnpm build` 已通过。
- **说明**：17.1 / 17.2 是内部开发编号，不作为正式版本号、Git tag 或 Release 名称；最终仍统一发布 v0.17.0。

## v0.17.0 开发阶段：17.2 图片墙效率增强测试记录

- **版本线**：v0.17.0
- **内部任务批次**：17.2
- **目标**：验证快捷键提示、批量分类和批量操作后选择行为配置。17.2 是内部开发编号，不作为正式版本号或 Git tag。

### 构建验证

- `pnpm build`：已通过。

### 配置兼容验证

- 从旧 `config.json` 启动时，应自动补齐 `gallery.batchSelectionBehavior = "clear"`。
- 修改批量操作后选择行为后，设置应写入 `config.json`，刷新后仍保持。
- 本轮不新增 SQLite 表或字段，旧数据库不需要迁移。

### 人工测试清单

- 图片墙筛选栏：确认不再显示筛选预设和快捷筛选入口，状态、星级、上传来源 / 上传者和搜索筛选仍正常。
- 快捷键提示：图片墙顶部可打开快捷键提示，预览弹窗也可打开；文案与实际快捷键一致，原 1-5、0、X、E、P、左右方向键、Esc 不受影响。
- 批量分类：选择多张图片，通过“更多操作 / 设置分类...”写入“合影”分类，确认所选图片分类均更新、操作日志记录操作者；留空提交时应清空分类。
- 批量操作后选择行为：设置为“清空选择”后批量改状态或批量分类成功应清空选择，部分失败时保留失败项；设置为“保留选择”后批量改状态或批量分类应保留当前选择。
- 回归：17.1 上传来源筛选、在线客户端显示、现场动态、客户端上传、JPG/JPEG/PNG 导入、任务中心、数据库备份、16.1 批量状态修改和筛选预览不退出均不回退。

## v0.17.0 开发阶段：17.1 现场协作感知与来源追踪测试记录

- **版本线**：v0.17.0
- **内部任务批次**：17.1
- **目标**：验证在线客户端感知、设备名持久化、上传来源追踪、上传者筛选、操作日志操作者和主机现场动态。17.1 是内部开发编号，不作为正式版本号或 Git tag。

### 构建验证

- `pnpm build`：已通过。

### 数据库迁移验证

- 从 v0.16.0 旧库启动时，应自动补齐 `images.uploaded_by_client_id`、`images.uploaded_by_name`、`images.uploaded_by_role`、`images.uploaded_at`。
- 从 v0.16.0 旧库启动时，应自动补齐 `operation_logs.actor_type`、`operation_logs.actor_id`、`operation_logs.actor_name`。
- 重复启动不应重复添加字段；字段已存在时迁移跳过。
- 迁移失败时日志应出现“数据库轻量迁移失败”，应用不得静默吞错。

### 人工测试清单

- 在线客户端：客户端连接主机，设备名设为“修图电脑A”；主机首页显示在线数量 1 和设备名；刷新客户端后设备名保留；断开后在线数量更新；两个客户端连接时显示两个设备。
- 上传来源：客户端“修图电脑A”上传 PNG 后图片墙实时出现；图片元数据 / 预览显示上传者“修图电脑A”；主机本地导入显示“主机导入”；旧图片无上传者时显示合理兜底。
- 上传者筛选：客户端 A 上传 2 张、客户端 B 上传 2 张、主机导入 2 张；筛选 A / B / 主机导入时只显示对应图片，分页 total 正确，并可与星级、状态、搜索组合。
- 操作日志归属：客户端 A 修改星级、客户端 B 修改状态、主机修改备注，`operation_logs.actor_name` 分别记录对应设备名或“主机”；批量状态修改逐图记录同一操作者。
- 现场动态：客户端上传、客户端改星级、主机改状态、删除 / 恢复、任务完成后，主机首页“现场动态”出现中文动态；最近动态最多保留最近若干条，刷新后可丢失但不报错。
- 回归：v0.16.0 导入、拖拽导入、客户端上传、任务中心、数据库备份、图片墙分页、批量状态修改、筛选预览不退出、星级精确筛选均不回退。

## v0.16.0 开发阶段：16.5 数据库备份与数据库位置迁移测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.5
- **目标**：验证数据库手动备份、启动自动备份、数据库位置迁移和迁移失败回滚，确认这些操作不会删除旧数据库或用户图片仓库。

### 重点验证项

- 设置仓库路径后点击“立即备份”，仓库下应生成 `metadata/database-backups/app-manual-YYYYMMDD-HHmmss.db`，UI 显示备份路径和文件大小。
- 未配置仓库路径时点击“立即备份”，应提示“请先配置仓库路径后再备份数据库”，应用不崩溃。
- 仓库不可写或路径失效时点击“立即备份”，应显示明确错误，后端不中断。
- 仓库可写时重启应用，启动自动备份应按 24 小时节流策略生成 `app-auto-YYYYMMDD-HHmmss.db`；24 小时内重复启动不应无限创建备份。
- 自动备份只清理超出保留数量的 `app-auto-*` 文件，不删除 `app-manual-*` 手动备份。
- 点击“迁移数据库位置”并选择新目录后，应先生成迁移前备份，再在目标目录生成 `app.db`，UI 提示新路径和“需要重启应用后生效”。
- 迁移成功后旧数据库文件仍保留，不应删除旧 `app.db`、`app.db-wal` 或 `app.db-shm`。
- 重启后 `/api/health` 与设置页应显示新的数据库路径，已有活动、图片记录和设置仍可读取。
- 迁移到不可写目录、已有目标文件或明显不合理目录时应失败，配置不应被写成坏路径，当前数据库仍可用。
- 数据库路径和图片仓库路径应在 UI 与文档中保持分离，迁移数据库不得影响 `working/`、`archive/` 或用户原图。

### 回归测试清单

- 创建活动、导入 JPG/JPEG/PNG、图片墙显示、客户端连接、导出发布、活动归档和任务中心均保持正常。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。
- 16.2 回归：导入页右侧处理结果与任务中心一致。
- 16.3 回归：已修图回传和任务中心覆盖不回退。
- 16.4 回归：主机拖拽导入和客户端拖拽上传仍正常。

## v0.16.0 开发阶段：16.4 拖拽导入 / 拖拽上传体验增强测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.4
- **目标**：验证主机导入页拖拽图片文件 / 文件夹、客户端上传页拖拽图片文件，并确认拖拽入口复用现有导入、上传和任务中心流程。

### 重点验证项

- 主机拖拽 1 张 JPG 到导入区，开始导入后任务中心出现 `host_import`，导入页右侧结果与任务中心一致，图片墙显示正常。
- 主机拖拽 1 张 PNG 到导入区，确认原图保留 PNG，缩略图和预览图生成 WebP。
- 主机拖拽多张 JPG/JPEG/PNG 图片文件，确认只导入拖入的文件，同一活动内重复图片计入 skipped。
- 主机拖拽文件夹，确认沿用文件夹导入逻辑，只扫描第一层 JPG/JPEG/PNG，不递归子目录。
- 主机拖拽包含 JPG/JPEG/PNG/TXT/GIF 的混合文件夹，确认不支持格式被跳过或提示，不导致整批导入失败。
- 主机拖拽空文件夹或无支持图片的文件夹，显示“未找到支持图片”类提示，不崩溃。
- 客户端拖拽 1 张 PNG 到上传区，上传成功后主机图片墙通过 Socket.IO 实时出现新图。
- 客户端拖拽多张 JPG/JPEG/PNG 到上传区，确认后端处理任务可见，成功 / 跳过 / 失败统计正常。
- 客户端拖拽文件夹，显示“客户端暂不支持拖拽文件夹”类提示，不崩溃。

### 回归测试清单

- 主机“选择文件夹”导入仍正常。
- 主机“选择图片文件”导入仍正常。
- 客户端点击选择文件上传仍正常。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。
- 16.2 回归：主机导入页右侧处理结果与任务中心一致。
- 16.3 回归：已修图回传和任务中心覆盖不回退。

## v0.16.0 开发阶段：16.3 任务中心覆盖补齐测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.3
- **目标**：验证上传、导出、下载、待修包、已修图回传和归档清理等耗时操作均能在统一任务中心看到进度、结果、错误和下载入口。

### 已覆盖任务类型

- 主机导入：`host_import`。
- 客户端上传后端处理：`client_upload_import`。
- 批量 ZIP 下载：`download_zip`。
- 发布导出：`publish_export`。
- 待修包生成：`edit_package`。
- 已修图回传：`edited_upload`。
- 归档生成：`archive_prepare`。
- 归档清理工作区：`archive_cleanup`。

### 回归测试清单

- 客户端上传多张 JPG/JPEG/PNG 后，任务中心显示后端处理任务，页面结果与任务中心一致，主机图片墙实时同步。
- 批量下载原图、预览图和 best ZIP，任务中心显示进度，完成后可通过任务结果下载 ZIP，缺失文件进入 errors。
- 发布导出多张图片，任务中心显示发布导出任务，导出页仍显示输出目录、ZIP 下载和错误列表。
- 生成单包、平均拆包和自定义待修包，任务中心可见待修包任务，生成后主机和客户端待修包列表可下载。
- 主机端和客户端端回传多张已修图，任务中心显示 `edited_upload` 进度；成功后图片状态变为 `edited`，缩略图 / 预览图刷新。
- 归档清理工作区时，任务中心显示清理进度，清理完成后归档页自动更新；文件占用错误进入 errors。
- 16.2 回归：主机导入页右侧处理结果与任务中心一致。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。

## v0.16.0 开发阶段：16.2 任务中心一致性测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.2
- **目标**：验证业务页面结果面板和任务中心使用同一任务状态来源，避免任务中心已完成但页面仍显示 0/0/0。

### 重点验证项

- 主机导入 15 张图片：任务中心显示 15/15，导入页右侧“处理结果”同步显示成功 15、失败 0、跳过 0。
- 主机导入重复图片：任务中心和导入页右侧 skipped 数量一致。
- 主机导入包含不支持文件或异常图片：任务中心和导入页右侧 failed / errors 一致。
- 大批量导入：导入页右侧进度条、已处理 / 总数、成功 / 失败 / 跳过随 `task-updated` 实时变化，不阻塞页面。
- 取消导入任务：任务中心显示 `cancelled`，导入页右侧显示已取消和已处理数量。
- 客户端多图上传：客户端上传页显示后端处理任务的总数、已处理、成功、跳过、失败；任务中心结果一致。
- 批量 ZIP 下载、待修包生成、发布导出、归档 prepare / cleanup：任务中心可见任务完成结果和错误摘要；原业务页面结果不回退。
- 16.1 回归：客户端新图加载、批量状态修改、筛选预览不退出、星级等于 / 及以上筛选仍正常。

## v0.16.0 开发阶段：16.1 实战问题修复测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.1
- **目标**：修复实战选片中的实时预览、批量状态、筛选预览和星级筛选体验问题。

### 本阶段重点

- 客户端收到 `image-created` 后，缩略图和预览图加载失败时会有限重试，不再需要刷新或重启客户端。
- 实时插入 / 更新图片时为缩略图和预览图追加版本参数，降低失败响应或旧资源缓存影响。
- 批量状态修改改为基于当前选中快照逐张执行，单张失败不阻断其他图片。
- 预览弹窗打开时冻结当前筛选结果快照，状态修改后不再立即关闭预览。
- 星级筛选增加 `ratingMode`，支持“等于某星级”和“某星级及以上”。

### 回归测试清单

- 主机导入或客户端上传 3 张新图，客户端图片墙无需刷新即可显示缩略图。
- 客户端打开新图预览弹窗，预览图首次失败时应自动重试并恢复。
- 选择 10 张图片批量改为“待修图”，确认 10 张全部更新；再批量改为“废片”，确认 10 张全部更新。
- 批量状态修改时如某张失败，其他图片仍应继续更新，并显示成功 / 失败数量。
- 筛选“待修图”后打开预览，把当前图改为“废片”，预览弹窗不关闭，左右切换仍使用打开时的图片快照。
- 关闭预览后，图片墙按当前筛选条件刷新，已改为“废片”的图片不再出现在“待修图”结果中。
- 设置 1 星、2 星、3 星、4 星、5 星图片，确认“等于 3 星”只显示 3 星，“3 星及以上”显示 3 / 4 / 5 星。
- 图片墙分页、JPG/JPEG/PNG 导入、客户端上传、任务中心不受影响。

## v0.15.0-rc.0 发布候选回归测试记录

- **版本**：v0.15.0-rc.0
- **发布类型**：Windows ZIP 便携发布候选版 / Pre-release
- **推荐交付物**：`MediaPhotoWorkbench-v0.15.0-rc.0-x64.zip`
- **使用方式**：解压 ZIP 后双击 `Media Photo Workbench.exe`

### 本阶段重点

- 主机文件夹导入和指定图片文件导入均支持 JPG/JPEG/PNG。
- 客户端上传支持单张或多张 JPG/JPEG/PNG。
- 大批量导入接入任务中心，显示已处理 / 总数、成功、跳过、失败、错误列表和取消入口。
- 大批量导入采用有限并发处理，避免完全串行或一次性 `Promise.all` 撑爆内存。
- 图片墙分页修复，超过 200 张图片时可继续加载 / 翻页查看剩余图片。
- 归档清理工作区改为后台任务，任务中心显示清理进度。
- 关闭程序时导入任务继续写日志导致 `pino` / `thread-stream worker exited` 的崩溃风险已修复。
- ZIP 便携包仍是推荐交付物；NSIS 安装包仍暂不推荐。

### 已通过人工测试

- 主机导入 JPG/JPEG/PNG 核心流程。
- 主机选择文件夹和选择图片文件两种导入入口。
- 客户端 JPG/JPEG/PNG 上传。
- 图片墙分页显示。
- 归档清理工作区任务化。
- `pnpm build` 已通过。

## v0.13.2-dev 发布后测试记录

- **版本**：v0.13.2-dev
- **发布类型**：Windows ZIP 便携预发布版
- **GitHub Release**：https://github.com/FFocalors/media-photo-workbench/releases/tag/v0.13.2-dev
- **推荐交付物**：`MediaPhotoWorkbench-v0.13.2-dev-x64.zip`
- **使用方式**：解压 ZIP 后双击 `Media Photo Workbench.exe`

### 已完成测试

- GitHub Release 已发布，Release 类型为 Pre-release。
- Windows ZIP 便携包已发布，解压后主程序可正常启动。
- 本机测试通过。
- 多设备局域网测试通过。
- 压力测试通过。
- 主机 Windows 热点测试通过。
- 主机首页真实端口、局域网地址、剩余空间和二维码可用。
- 客户端可通过主机首页复制局域网地址或扫描二维码访问主机。
- 核心流程已验证：主机导入、客户端上传、图片墙选片、实时同步、待修包、已修图回传、导出发布、轻量归档。

### 已知限制

- 当前导入与客户端上传支持 JPG/JPEG/PNG；已修图回传仍建议使用 JPG/JPEG 成片。
- 暂不支持 RAW / HEIC / 视频。
- 暂不支持远程传输、云同步、ngrok、FTP/SFTP。
- 校园网可能存在设备隔离；即使连接同一 Wi-Fi，客户端也可能无法访问主机。
- 局域网连接失败时，推荐使用主机 Windows 热点，常见主机地址为 `192.168.137.1`。
- 单文件 portable EXE 暂不作为推荐交付物。
- NSIS 安装包已在 v0.14.0 初步人工验证中发现安装进度卡住、取消按钮无响应，当前暂不推荐作为交付物。

## v0.14.0 线下补测清单

- 防火墙 / 热点 / 校园网连接排查提示。
- 错误日志导出入口。
- 一键打开日志目录。
- NSIS 安装包后续重新评估；当前不作为 v0.14.0 推荐交付物或阻塞项。
- 多客户端并发上传复测。
- 真实活动压力测试复核，建议覆盖 50 / 300 / 500 张 JPG/JPEG/PNG。
- 启动速度记录与优化。
- README 故障排查章节完善。

## v0.15.0-rc.0 PNG 支持补测清单

- 主机本地导入测试文件夹包含 JPG、JPEG、PNG 和不支持文件；扫描结果只统计 JPG/JPEG/PNG。
- 主机导入页使用“选择图片文件”只选择部分 JPG/JPEG/PNG，确认只导入被选中的图片，未选中的同目录图片不应入库。
- PNG 导入后，原图目录保留 PNG 原图，缩略图和预览图目录生成对应 WebP。
- 图片墙能显示 PNG 缩略图，预览弹窗能显示 PNG 生成的预览图。
- 客户端上传 PNG 后，主机图片墙通过 Socket.IO 实时出现新图。
- 同一活动重复上传同一 PNG 应计入 skipped，不同活动允许导入同一 PNG。
- PNG 原图下载正常，批量 ZIP 的 original / best 能包含 PNG 原图。
- 待修包选择 PNG 图片时，ZIP 内 `待修原图/` 保留 PNG 原图，`edit_manifest.json` 记录正确文件名。
- 发布导出选择 PNG 图片时，按当前发布策略输出 JPG 发布图，不破坏 JPG/JPEG 导出。

## v0.15.0-rc.0 大批量导入任务化补测清单

- 使用 4000 张 JPG/JPEG/PNG 混合素材执行主机文件夹导入，确认接口快速返回任务，任务中心显示导入任务。
- 导入过程中任务中心应显示已处理 / 总数、成功、跳过、失败、已用时间和预计剩余时间。
- 导入过程中图片墙应随 `image-created` 逐步出现新图，UI 不应假死。
- 中途点击任务中心“取消”，确认任务进入 `cancelled`，已成功导入图片保留，未处理图片停止导入。
- 导入过程中关闭应用，确认不再出现 `Error: the worker has exited` 或 Electron 主进程 JavaScript 错误弹窗。
- 重新启动后，关闭前已成功导入的图片仍可在图片墙查看。
- 重复导入同一批素材，确认同一活动内快速计入 skipped，不重复生成缩略图 / 预览图。
- 客户端一次上传多张 JPG/JPEG/PNG，确认上传后进入后台处理任务，主机和客户端任务中心可见进度。

## v0.15.0-rc.0 提交前完整回归测试清单

> 本清单用于本阶段提交前和发布候选包生成前复核。当前仍以 Windows ZIP 便携包为推荐交付物，NSIS 安装包暂不推荐。

### 1. 主机导入

- 主机文件夹导入 JPG/JPEG/PNG：准备包含 JPG、JPEG、PNG、GIF/TXT 的目录，选择文件夹扫描，确认只统计 JPG/JPEG/PNG。
- 主机文件夹导入任务化：开始导入后接口快速返回，任务中心显示导入任务、进度、成功、跳过、失败、已用时间和预计剩余时间。
- 主机选择图片文件导入：切换到“选择图片文件”模式时不应直接弹出资源管理器；点击“浏览图片”后可多选图片；只导入被选中的文件。
- 重复导入：同一活动内重复导入同一 JPG/JPEG/PNG 应计入 skipped；不同活动允许导入同一张图片。
- PNG 处理：PNG 原图保留 PNG，缩略图和预览图生成 WebP，图片墙和预览弹窗显示正常。

### 2. 客户端上传

- 客户端上传 JPG/JPEG/PNG：可单选或多选上传，上传后主机图片墙通过 Socket.IO 实时出现新图。
- 客户端上传任务化：多文件上传后任务中心显示处理任务和进度。
- 客户端重复上传：同一活动内重复 PNG/JPG/JPEG 应计入 skipped，不重复生成资源。

### 3. 大批量导入稳定性

- 4000 张大批量导入：使用 JPG/JPEG/PNG 混合素材，确认 UI 不假死，任务中心进度持续更新。
- ETA：已处理数量大于 0 后预计剩余时间应随处理进度逐步更新，不应长期显示明显异常值。
- 导入任务取消：导入中点击取消，任务进入 `cancelled`；已导入图片保留，未处理图片停止导入。
- 导入中关闭程序：关闭应用不应出现 Electron 主进程 JavaScript 错误或 `pino / thread-stream worker exited` 弹窗。
- 重启后数据保留：重新启动后，关闭前已成功导入的图片、活动和仓库配置仍可读取。

### 4. 图片墙与选片

- 图片墙分页 / 加载更多：超过 200 张图片时可继续加载或翻页查看剩余图片，总数显示正确。
- 图片筛选：星级、状态、来源、关键字筛选可用。
- 图片操作：打星、状态、分类、备注修改后当前页面和其他客户端实时同步。
- 预览弹窗：可打开预览图，快捷键 1-5、0、X、E、P、左右切换、Esc 关闭可用。

### 5. 下载与 ZIP

- 单图下载：原图、预览图下载正常；PNG 原图下载保持 PNG。
- 批量 ZIP 下载：选择多图生成 ZIP，任务中心显示进度，完成后可下载。
- 缺失文件：原图或已修图缺失时进入任务 errors，不应导致整个任务崩溃。

### 6. 修图流转

- 待修包：将图片标记为待修图后，可生成单包、平均拆包、自定义分包；ZIP 内包含 `edit_manifest.json`、`待修原图/`、`已修图回传/`。
- 待修包删除：主机端可删除误生成的待修包记录和 ZIP，不影响图片状态、原图或已修图。
- 已修图回传：上传 JPG/JPEG 成片，manifest 优先匹配，文件名兜底匹配；成功后状态变为 `edited`，缩略图和预览图刷新。
- 重复回传：同一张图重复回传只保留最新已修版本。

### 7. 导出发布

- 按可发布 / 已修图 / 4 星及以上 / 手动选择导出。
- 规格：原尺寸、长边 3000px、长边 1920px 均可执行。
- PNG 兼容：选择 PNG 原图导出时按当前策略输出 JPG 发布图，不破坏 JPG/JPEG 导出。
- ZIP 下载：发布 ZIP 生成后可下载，导出任务和日志记录正常。

### 8. 活动归档

- 活动归档：生成轻量归档，确认缩略图、`manifest.json`、`images.csv`、`operation_logs.csv`、`event.db` 生成。
- 归档验证：验证通过后才允许清理工作区。
- 归档清理工作区任务：点击清理后页面不应卡住，任务中心显示 `archive_cleanup` 进度；完成后 `working/{event_slug}` 被清理。
- 归档只读打开：工作区清理后仍可从归档页查看活动摘要和缩略图。
- 归档删除：归档目录删除需要二次确认，不应误删其他活动或仓库根目录。

### 9. 回收站与删除

- 图片逻辑删除后进入图片回收站，恢复后重新出现在图片墙。
- 图片永久删除仅允许对已逻辑删除图片执行，删除前有二次确认。
- 活动逻辑删除后进入活动回收站，恢复后重新进入活动列表。
- 活动永久删除应清理对应 working / archive 相关内容和数据库记录，不影响其他活动。

### 10. 连接与诊断

- 主机首页显示真实端口、WLAN / 以太网地址、剩余空间和二维码，不显示 VMware / Docker / WSL / Hyper-V 虚拟网卡。
- 客户端连接正确地址成功；非法地址、网络无响应、端口无服务、API `ok=false` 分别显示明确排查建议。
- 系统设置“故障排查 / 诊断信息”可打开日志目录并复制诊断信息。
- 校园网不通时使用 Windows 热点，常见地址 `192.168.137.1` 可作为补测路径。

### 11. 构建与交付

- `pnpm build` 通过。
- 生成 ZIP 便携包后，解压运行 `Media Photo Workbench.exe` 正常。
- 生产模式不依赖 Vite `5173`，前端 / API / Socket.IO 统一走真实后端端口。
- `dist/`、`dist-server/`、`release*`、ZIP、EXE、数据库、日志、真实图片、`working/`、`archive/` 不进入 Git。
