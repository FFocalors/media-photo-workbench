# Media Photo Workbench / 融媒体图片工作台 - 测试记录

## v0.17.0 阶段测试汇总

- **状态**：17.1 与 17.2 已完成并测试通过。
- **确认时间**：2026-05-24。
- **构建验证**：最近一次 `pnpm build` 已通过。
- **说明**：17.1 / 17.2 是内部开发编号，不作为正式版本号、Git tag 或 Release 名称；最终仍统一发布 v0.17.0。

## v0.17.0 开发阶段：17.2 图片墙效率增强测试记录

- **版本线**：v0.17.0
- **内部任务批次**：17.2
- **目标**：验证快捷键提示、批量分类和批量操作后选择行为配置。17.2 是内部开发编号，不作为正式版本号或 Git tag。

### 构建验证

- `pnpm build`：已通过。

### 配置兼容验证

- 从旧 `config.json` 启动时，应自动补齐 `gallery.batchSelectionBehavior = "clear"`。
- 修改批量操作后选择行为后，设置应写入 `config.json`，刷新后仍保持。
- 本轮不新增 SQLite 表或字段，旧数据库不需要迁移。

### 人工测试清单

- 图片墙筛选栏：确认不再显示筛选预设和快捷筛选入口，状态、星级、上传来源 / 上传者和搜索筛选仍正常。
- 快捷键提示：图片墙顶部可打开快捷键提示，预览弹窗也可打开；文案与实际快捷键一致，原 1-5、0、X、E、P、左右方向键、Esc 不受影响。
- 批量分类：选择多张图片，通过“更多操作 / 设置分类...”写入“合影”分类，确认所选图片分类均更新、操作日志记录操作者；留空提交时应清空分类。
- 批量操作后选择行为：设置为“清空选择”后批量改状态或批量分类成功应清空选择，部分失败时保留失败项；设置为“保留选择”后批量改状态或批量分类应保留当前选择。
- 回归：17.1 上传来源筛选、在线客户端显示、现场动态、客户端上传、JPG/JPEG/PNG 导入、任务中心、数据库备份、16.1 批量状态修改和筛选预览不退出均不回退。

## v0.17.0 开发阶段：17.1 现场协作感知与来源追踪测试记录

- **版本线**：v0.17.0
- **内部任务批次**：17.1
- **目标**：验证在线客户端感知、设备名持久化、上传来源追踪、上传者筛选、操作日志操作者和主机现场动态。17.1 是内部开发编号，不作为正式版本号或 Git tag。

### 构建验证

- `pnpm build`：已通过。

### 数据库迁移验证

- 从 v0.16.0 旧库启动时，应自动补齐 `images.uploaded_by_client_id`、`images.uploaded_by_name`、`images.uploaded_by_role`、`images.uploaded_at`。
- 从 v0.16.0 旧库启动时，应自动补齐 `operation_logs.actor_type`、`operation_logs.actor_id`、`operation_logs.actor_name`。
- 重复启动不应重复添加字段；字段已存在时迁移跳过。
- 迁移失败时日志应出现“数据库轻量迁移失败”，应用不得静默吞错。

### 人工测试清单

- 在线客户端：客户端连接主机，设备名设为“修图电脑A”；主机首页显示在线数量 1 和设备名；刷新客户端后设备名保留；断开后在线数量更新；两个客户端连接时显示两个设备。
- 上传来源：客户端“修图电脑A”上传 PNG 后图片墙实时出现；图片元数据 / 预览显示上传者“修图电脑A”；主机本地导入显示“主机导入”；旧图片无上传者时显示合理兜底。
- 上传者筛选：客户端 A 上传 2 张、客户端 B 上传 2 张、主机导入 2 张；筛选 A / B / 主机导入时只显示对应图片，分页 total 正确，并可与星级、状态、搜索组合。
- 操作日志归属：客户端 A 修改星级、客户端 B 修改状态、主机修改备注，`operation_logs.actor_name` 分别记录对应设备名或“主机”；批量状态修改逐图记录同一操作者。
- 现场动态：客户端上传、客户端改星级、主机改状态、删除 / 恢复、任务完成后，主机首页“现场动态”出现中文动态；最近动态最多保留最近若干条，刷新后可丢失但不报错。
- 回归：v0.16.0 导入、拖拽导入、客户端上传、任务中心、数据库备份、图片墙分页、批量状态修改、筛选预览不退出、星级精确筛选均不回退。

## v0.16.0 开发阶段：16.5 数据库备份与数据库位置迁移测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.5
- **目标**：验证数据库手动备份、启动自动备份、数据库位置迁移和迁移失败回滚，确认这些操作不会删除旧数据库或用户图片仓库。

### 重点验证项

- 设置仓库路径后点击“立即备份”，仓库下应生成 `metadata/database-backups/app-manual-YYYYMMDD-HHmmss.db`，UI 显示备份路径和文件大小。
- 未配置仓库路径时点击“立即备份”，应提示“请先配置仓库路径后再备份数据库”，应用不崩溃。
- 仓库不可写或路径失效时点击“立即备份”，应显示明确错误，后端不中断。
- 仓库可写时重启应用，启动自动备份应按 24 小时节流策略生成 `app-auto-YYYYMMDD-HHmmss.db`；24 小时内重复启动不应无限创建备份。
- 自动备份只清理超出保留数量的 `app-auto-*` 文件，不删除 `app-manual-*` 手动备份。
- 点击“迁移数据库位置”并选择新目录后，应先生成迁移前备份，再在目标目录生成 `app.db`，UI 提示新路径和“需要重启应用后生效”。
- 迁移成功后旧数据库文件仍保留，不应删除旧 `app.db`、`app.db-wal` 或 `app.db-shm`。
- 重启后 `/api/health` 与设置页应显示新的数据库路径，已有活动、图片记录和设置仍可读取。
- 迁移到不可写目录、已有目标文件或明显不合理目录时应失败，配置不应被写成坏路径，当前数据库仍可用。
- 数据库路径和图片仓库路径应在 UI 与文档中保持分离，迁移数据库不得影响 `working/`、`archive/` 或用户原图。

### 回归测试清单

- 创建活动、导入 JPG/JPEG/PNG、图片墙显示、客户端连接、导出发布、活动归档和任务中心均保持正常。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。
- 16.2 回归：导入页右侧处理结果与任务中心一致。
- 16.3 回归：已修图回传和任务中心覆盖不回退。
- 16.4 回归：主机拖拽导入和客户端拖拽上传仍正常。

## v0.16.0 开发阶段：16.4 拖拽导入 / 拖拽上传体验增强测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.4
- **目标**：验证主机导入页拖拽图片文件 / 文件夹、客户端上传页拖拽图片文件，并确认拖拽入口复用现有导入、上传和任务中心流程。

### 重点验证项

- 主机拖拽 1 张 JPG 到导入区，开始导入后任务中心出现 `host_import`，导入页右侧结果与任务中心一致，图片墙显示正常。
- 主机拖拽 1 张 PNG 到导入区，确认原图保留 PNG，缩略图和预览图生成 WebP。
- 主机拖拽多张 JPG/JPEG/PNG 图片文件，确认只导入拖入的文件，同一活动内重复图片计入 skipped。
- 主机拖拽文件夹，确认沿用文件夹导入逻辑，只扫描第一层 JPG/JPEG/PNG，不递归子目录。
- 主机拖拽包含 JPG/JPEG/PNG/TXT/GIF 的混合文件夹，确认不支持格式被跳过或提示，不导致整批导入失败。
- 主机拖拽空文件夹或无支持图片的文件夹，显示“未找到支持图片”类提示，不崩溃。
- 客户端拖拽 1 张 PNG 到上传区，上传成功后主机图片墙通过 Socket.IO 实时出现新图。
- 客户端拖拽多张 JPG/JPEG/PNG 到上传区，确认后端处理任务可见，成功 / 跳过 / 失败统计正常。
- 客户端拖拽文件夹，显示“客户端暂不支持拖拽文件夹”类提示，不崩溃。

### 回归测试清单

- 主机“选择文件夹”导入仍正常。
- 主机“选择图片文件”导入仍正常。
- 客户端点击选择文件上传仍正常。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。
- 16.2 回归：主机导入页右侧处理结果与任务中心一致。
- 16.3 回归：已修图回传和任务中心覆盖不回退。

## v0.16.0 开发阶段：16.3 任务中心覆盖补齐测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.3
- **目标**：验证上传、导出、下载、待修包、已修图回传和归档清理等耗时操作均能在统一任务中心看到进度、结果、错误和下载入口。

### 已覆盖任务类型

- 主机导入：`host_import`。
- 客户端上传后端处理：`client_upload_import`。
- 批量 ZIP 下载：`download_zip`。
- 发布导出：`publish_export`。
- 待修包生成：`edit_package`。
- 已修图回传：`edited_upload`。
- 归档生成：`archive_prepare`。
- 归档清理工作区：`archive_cleanup`。

### 回归测试清单

- 客户端上传多张 JPG/JPEG/PNG 后，任务中心显示后端处理任务，页面结果与任务中心一致，主机图片墙实时同步。
- 批量下载原图、预览图和 best ZIP，任务中心显示进度，完成后可通过任务结果下载 ZIP，缺失文件进入 errors。
- 发布导出多张图片，任务中心显示发布导出任务，导出页仍显示输出目录、ZIP 下载和错误列表。
- 生成单包、平均拆包和自定义待修包，任务中心可见待修包任务，生成后主机和客户端待修包列表可下载。
- 主机端和客户端端回传多张已修图，任务中心显示 `edited_upload` 进度；成功后图片状态变为 `edited`，缩略图 / 预览图刷新。
- 归档清理工作区时，任务中心显示清理进度，清理完成后归档页自动更新；文件占用错误进入 errors。
- 16.2 回归：主机导入页右侧处理结果与任务中心一致。
- 16.1 回归：新图客户端预览加载、批量状态修改、筛选预览不退出、星级精确筛选仍正常。

## v0.16.0 开发阶段：16.2 任务中心一致性测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.2
- **目标**：验证业务页面结果面板和任务中心使用同一任务状态来源，避免任务中心已完成但页面仍显示 0/0/0。

### 重点验证项

- 主机导入 15 张图片：任务中心显示 15/15，导入页右侧“处理结果”同步显示成功 15、失败 0、跳过 0。
- 主机导入重复图片：任务中心和导入页右侧 skipped 数量一致。
- 主机导入包含不支持文件或异常图片：任务中心和导入页右侧 failed / errors 一致。
- 大批量导入：导入页右侧进度条、已处理 / 总数、成功 / 失败 / 跳过随 `task-updated` 实时变化，不阻塞页面。
- 取消导入任务：任务中心显示 `cancelled`，导入页右侧显示已取消和已处理数量。
- 客户端多图上传：客户端上传页显示后端处理任务的总数、已处理、成功、跳过、失败；任务中心结果一致。
- 批量 ZIP 下载、待修包生成、发布导出、归档 prepare / cleanup：任务中心可见任务完成结果和错误摘要；原业务页面结果不回退。
- 16.1 回归：客户端新图加载、批量状态修改、筛选预览不退出、星级等于 / 及以上筛选仍正常。

## v0.16.0 开发阶段：16.1 实战问题修复测试记录

- **版本线**：v0.16.0
- **内部任务批次**：16.1
- **目标**：修复实战选片中的实时预览、批量状态、筛选预览和星级筛选体验问题。

### 本阶段重点

- 客户端收到 `image-created` 后，缩略图和预览图加载失败时会有限重试，不再需要刷新或重启客户端。
- 实时插入 / 更新图片时为缩略图和预览图追加版本参数，降低失败响应或旧资源缓存影响。
- 批量状态修改改为基于当前选中快照逐张执行，单张失败不阻断其他图片。
- 预览弹窗打开时冻结当前筛选结果快照，状态修改后不再立即关闭预览。
- 星级筛选增加 `ratingMode`，支持“等于某星级”和“某星级及以上”。

### 回归测试清单

- 主机导入或客户端上传 3 张新图，客户端图片墙无需刷新即可显示缩略图。
- 客户端打开新图预览弹窗，预览图首次失败时应自动重试并恢复。
- 选择 10 张图片批量改为“待修图”，确认 10 张全部更新；再批量改为“废片”，确认 10 张全部更新。
- 批量状态修改时如某张失败，其他图片仍应继续更新，并显示成功 / 失败数量。
- 筛选“待修图”后打开预览，把当前图改为“废片”，预览弹窗不关闭，左右切换仍使用打开时的图片快照。
- 关闭预览后，图片墙按当前筛选条件刷新，已改为“废片”的图片不再出现在“待修图”结果中。
- 设置 1 星、2 星、3 星、4 星、5 星图片，确认“等于 3 星”只显示 3 星，“3 星及以上”显示 3 / 4 / 5 星。
- 图片墙分页、JPG/JPEG/PNG 导入、客户端上传、任务中心不受影响。

## v0.15.0-rc.0 发布候选回归测试记录

- **版本**：v0.15.0-rc.0
- **发布类型**：Windows ZIP 便携发布候选版 / Pre-release
- **推荐交付物**：`MediaPhotoWorkbench-v0.15.0-rc.0-x64.zip`
- **使用方式**：解压 ZIP 后双击 `Media Photo Workbench.exe`

### 本阶段重点

- 主机文件夹导入和指定图片文件导入均支持 JPG/JPEG/PNG。
- 客户端上传支持单张或多张 JPG/JPEG/PNG。
- 大批量导入接入任务中心，显示已处理 / 总数、成功、跳过、失败、错误列表和取消入口。
- 大批量导入采用有限并发处理，避免完全串行或一次性 `Promise.all` 撑爆内存。
- 图片墙分页修复，超过 200 张图片时可继续加载 / 翻页查看剩余图片。
- 归档清理工作区改为后台任务，任务中心显示清理进度。
- 关闭程序时导入任务继续写日志导致 `pino` / `thread-stream worker exited` 的崩溃风险已修复。
- ZIP 便携包仍是推荐交付物；NSIS 安装包仍暂不推荐。

### 已通过人工测试

- 主机导入 JPG/JPEG/PNG 核心流程。
- 主机选择文件夹和选择图片文件两种导入入口。
- 客户端 JPG/JPEG/PNG 上传。
- 图片墙分页显示。
- 归档清理工作区任务化。
- `pnpm build` 已通过。

## v0.13.2-dev 发布后测试记录

- **版本**：v0.13.2-dev
- **发布类型**：Windows ZIP 便携预发布版
- **GitHub Release**：https://github.com/FFocalors/media-photo-workbench/releases/tag/v0.13.2-dev
- **推荐交付物**：`MediaPhotoWorkbench-v0.13.2-dev-x64.zip`
- **使用方式**：解压 ZIP 后双击 `Media Photo Workbench.exe`

### 已完成测试

- GitHub Release 已发布，Release 类型为 Pre-release。
- Windows ZIP 便携包已发布，解压后主程序可正常启动。
- 本机测试通过。
- 多设备局域网测试通过。
- 压力测试通过。
- 主机 Windows 热点测试通过。
- 主机首页真实端口、局域网地址、剩余空间和二维码可用。
- 客户端可通过主机首页复制局域网地址或扫描二维码访问主机。
- 核心流程已验证：主机导入、客户端上传、图片墙选片、实时同步、待修包、已修图回传、导出发布、轻量归档。

### 已知限制

- 当前导入与客户端上传支持 JPG/JPEG/PNG；已修图回传仍建议使用 JPG/JPEG 成片。
- 暂不支持 RAW / HEIC / 视频。
- 暂不支持远程传输、云同步、ngrok、FTP/SFTP。
- 校园网可能存在设备隔离；即使连接同一 Wi-Fi，客户端也可能无法访问主机。
- 局域网连接失败时，推荐使用主机 Windows 热点，常见主机地址为 `192.168.137.1`。
- 单文件 portable EXE 暂不作为推荐交付物。
- NSIS 安装包已在 v0.14.0 初步人工验证中发现安装进度卡住、取消按钮无响应，当前暂不推荐作为交付物。

## v0.14.0 线下补测清单

- 防火墙 / 热点 / 校园网连接排查提示。
- 错误日志导出入口。
- 一键打开日志目录。
- NSIS 安装包后续重新评估；当前不作为 v0.14.0 推荐交付物或阻塞项。
- 多客户端并发上传复测。
- 真实活动压力测试复核，建议覆盖 50 / 300 / 500 张 JPG/JPEG/PNG。
- 启动速度记录与优化。
- README 故障排查章节完善。

## v0.15.0-rc.0 PNG 支持补测清单

- 主机本地导入测试文件夹包含 JPG、JPEG、PNG 和不支持文件；扫描结果只统计 JPG/JPEG/PNG。
- 主机导入页使用“选择图片文件”只选择部分 JPG/JPEG/PNG，确认只导入被选中的图片，未选中的同目录图片不应入库。
- PNG 导入后，原图目录保留 PNG 原图，缩略图和预览图目录生成对应 WebP。
- 图片墙能显示 PNG 缩略图，预览弹窗能显示 PNG 生成的预览图。
- 客户端上传 PNG 后，主机图片墙通过 Socket.IO 实时出现新图。
- 同一活动重复上传同一 PNG 应计入 skipped，不同活动允许导入同一 PNG。
- PNG 原图下载正常，批量 ZIP 的 original / best 能包含 PNG 原图。
- 待修包选择 PNG 图片时，ZIP 内 `待修原图/` 保留 PNG 原图，`edit_manifest.json` 记录正确文件名。
- 发布导出选择 PNG 图片时，按当前发布策略输出 JPG 发布图，不破坏 JPG/JPEG 导出。

## v0.15.0-rc.0 大批量导入任务化补测清单

- 使用 4000 张 JPG/JPEG/PNG 混合素材执行主机文件夹导入，确认接口快速返回任务，任务中心显示导入任务。
- 导入过程中任务中心应显示已处理 / 总数、成功、跳过、失败、已用时间和预计剩余时间。
- 导入过程中图片墙应随 `image-created` 逐步出现新图，UI 不应假死。
- 中途点击任务中心“取消”，确认任务进入 `cancelled`，已成功导入图片保留，未处理图片停止导入。
- 导入过程中关闭应用，确认不再出现 `Error: the worker has exited` 或 Electron 主进程 JavaScript 错误弹窗。
- 重新启动后，关闭前已成功导入的图片仍可在图片墙查看。
- 重复导入同一批素材，确认同一活动内快速计入 skipped，不重复生成缩略图 / 预览图。
- 客户端一次上传多张 JPG/JPEG/PNG，确认上传后进入后台处理任务，主机和客户端任务中心可见进度。

## v0.15.0-rc.0 提交前完整回归测试清单

> 本清单用于本阶段提交前和发布候选包生成前复核。当前仍以 Windows ZIP 便携包为推荐交付物，NSIS 安装包暂不推荐。

### 1. 主机导入

- 主机文件夹导入 JPG/JPEG/PNG：准备包含 JPG、JPEG、PNG、GIF/TXT 的目录，选择文件夹扫描，确认只统计 JPG/JPEG/PNG。
- 主机文件夹导入任务化：开始导入后接口快速返回，任务中心显示导入任务、进度、成功、跳过、失败、已用时间和预计剩余时间。
- 主机选择图片文件导入：切换到“选择图片文件”模式时不应直接弹出资源管理器；点击“浏览图片”后可多选图片；只导入被选中的文件。
- 重复导入：同一活动内重复导入同一 JPG/JPEG/PNG 应计入 skipped；不同活动允许导入同一张图片。
- PNG 处理：PNG 原图保留 PNG，缩略图和预览图生成 WebP，图片墙和预览弹窗显示正常。

### 2. 客户端上传

- 客户端上传 JPG/JPEG/PNG：可单选或多选上传，上传后主机图片墙通过 Socket.IO 实时出现新图。
- 客户端上传任务化：多文件上传后任务中心显示处理任务和进度。
- 客户端重复上传：同一活动内重复 PNG/JPG/JPEG 应计入 skipped，不重复生成资源。

### 3. 大批量导入稳定性

- 4000 张大批量导入：使用 JPG/JPEG/PNG 混合素材，确认 UI 不假死，任务中心进度持续更新。
- ETA：已处理数量大于 0 后预计剩余时间应随处理进度逐步更新，不应长期显示明显异常值。
- 导入任务取消：导入中点击取消，任务进入 `cancelled`；已导入图片保留，未处理图片停止导入。
- 导入中关闭程序：关闭应用不应出现 Electron 主进程 JavaScript 错误或 `pino / thread-stream worker exited` 弹窗。
- 重启后数据保留：重新启动后，关闭前已成功导入的图片、活动和仓库配置仍可读取。

### 4. 图片墙与选片

- 图片墙分页 / 加载更多：超过 200 张图片时可继续加载或翻页查看剩余图片，总数显示正确。
- 图片筛选：星级、状态、来源、关键字筛选可用。
- 图片操作：打星、状态、分类、备注修改后当前页面和其他客户端实时同步。
- 预览弹窗：可打开预览图，快捷键 1-5、0、X、E、P、左右切换、Esc 关闭可用。

### 5. 下载与 ZIP

- 单图下载：原图、预览图下载正常；PNG 原图下载保持 PNG。
- 批量 ZIP 下载：选择多图生成 ZIP，任务中心显示进度，完成后可下载。
- 缺失文件：原图或已修图缺失时进入任务 errors，不应导致整个任务崩溃。

### 6. 修图流转

- 待修包：将图片标记为待修图后，可生成单包、平均拆包、自定义分包；ZIP 内包含 `edit_manifest.json`、`待修原图/`、`已修图回传/`。
- 待修包删除：主机端可删除误生成的待修包记录和 ZIP，不影响图片状态、原图或已修图。
- 已修图回传：上传 JPG/JPEG 成片，manifest 优先匹配，文件名兜底匹配；成功后状态变为 `edited`，缩略图和预览图刷新。
- 重复回传：同一张图重复回传只保留最新已修版本。

### 7. 导出发布

- 按可发布 / 已修图 / 4 星及以上 / 手动选择导出。
- 规格：原尺寸、长边 3000px、长边 1920px 均可执行。
- PNG 兼容：选择 PNG 原图导出时按当前策略输出 JPG 发布图，不破坏 JPG/JPEG 导出。
- ZIP 下载：发布 ZIP 生成后可下载，导出任务和日志记录正常。

### 8. 活动归档

- 活动归档：生成轻量归档，确认缩略图、`manifest.json`、`images.csv`、`operation_logs.csv`、`event.db` 生成。
- 归档验证：验证通过后才允许清理工作区。
- 归档清理工作区任务：点击清理后页面不应卡住，任务中心显示 `archive_cleanup` 进度；完成后 `working/{event_slug}` 被清理。
- 归档只读打开：工作区清理后仍可从归档页查看活动摘要和缩略图。
- 归档删除：归档目录删除需要二次确认，不应误删其他活动或仓库根目录。

### 9. 回收站与删除

- 图片逻辑删除后进入图片回收站，恢复后重新出现在图片墙。
- 图片永久删除仅允许对已逻辑删除图片执行，删除前有二次确认。
- 活动逻辑删除后进入活动回收站，恢复后重新进入活动列表。
- 活动永久删除应清理对应 working / archive 相关内容和数据库记录，不影响其他活动。

### 10. 连接与诊断

- 主机首页显示真实端口、WLAN / 以太网地址、剩余空间和二维码，不显示 VMware / Docker / WSL / Hyper-V 虚拟网卡。
- 客户端连接正确地址成功；非法地址、网络无响应、端口无服务、API `ok=false` 分别显示明确排查建议。
- 系统设置“故障排查 / 诊断信息”可打开日志目录并复制诊断信息。
- 校园网不通时使用 Windows 热点，常见地址 `192.168.137.1` 可作为补测路径。

### 11. 构建与交付

- `pnpm build` 通过。
- 生成 ZIP 便携包后，解压运行 `Media Photo Workbench.exe` 正常。
- 生产模式不依赖 Vite `5173`，前端 / API / Socket.IO 统一走真实后端端口。
- `dist/`、`dist-server/`、`release*`、ZIP、EXE、数据库、日志、真实图片、`working/`、`archive/` 不进入 Git。
